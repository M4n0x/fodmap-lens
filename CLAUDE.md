# CLAUDE.md

## Skills

- **install-android**: Use the `install-android` skill (`.claude/skills/install-android.md`) when the user asks to install or deploy the app on their Android device. Do NOT use `expo run:android` or `eas build`.
